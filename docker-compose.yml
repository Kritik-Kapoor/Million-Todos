services:
  postgres:
    image: postgres:17

    container_name: million-todos-db

    restart: unless-stopped

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: million-todos
      POSTGRES_DB: million_todos

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d million_todos"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
